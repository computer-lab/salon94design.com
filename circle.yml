machine:
  node:
    version: 8.6.0
dependencies:
  override:
    - npm i
test:
  override:
    - exit 0
deployment:
  release:
    branch: kevin-prevent-superlarge-image
    owner: computer-lab
    commands:
      - ./contrib/resize-images.sh
